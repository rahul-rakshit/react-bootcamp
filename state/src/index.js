import React from "react";
import ReactDOM from "react-dom";
import { SampleApp } from "./SampleComponent";

ReactDOM.render(
  <React.StrictMode>
    <SampleApp />
  </React.StrictMode>,
  document.getElementById("root")
);
